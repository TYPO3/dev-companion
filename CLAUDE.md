See [AGENTS.md](AGENTS.md) — the conventions for working on this repository are
tool-neutral and live there.

The line below hands that file to this client rather than sends it to fetch one. The client inlines an `@` path where it loads this file, once and cached. A session that has the conventions spends no call on them and no second one after every compaction. Measured over the twenty sessions
before 2026-08-08, eighteen read AGENTS.md whole and twenty-eight of those reads
were the same session reading it again.

@AGENTS.md
