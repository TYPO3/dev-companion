[
  {
    "id": "language-files",
    "title": "Language Files",
    "domains": [
      "xliff"
    ],
    "appliesTo": [
      ".xlf",
      ".xliff",
      "xlf",
      "xliff",
      "language file",
      "locallang",
      "label",
      "translation",
      "translate",
      "localization",
      "localisation",
      "lll:",
      "Resources/Private/Language/",
      "trans-unit",
      "source language",
      "not English",
      "en.locallang",
      "de.locallang",
      "locale prefix",
      "default locale",
      "fallback locale",
      "lll(",
      "lit-helper",
      "label in a JavaScript module"
    ],
    "hints": [
      "Use clear, stable label identifiers and concise wording.",
      "Author new labels in English in the source XLF. Put a translation in the locale-prefixed file beside it — for example de.locallang.xlf for locallang.xlf — with the same unit id. An existing source file whose labels are not English is a defect to report, not a convention to continue.",
      "Start at the consuming code or registration and identify the translation resource it already uses. Reuse a label only when the same meaning already exists in that resource and local context; a matching label elsewhere in the installation is not a reference to import across module or package boundaries. Call typo3_label_lookup with that resource rather than treating its instance-wide search as a shared vocabulary.",
      "When the local resource needs a new unit, make its id express the meaning at that use site and fit the resource's existing hierarchy — for example actions.createRecord or import.emptyState — rather than a context-free id such as new.",
      "Which trans-units exist is a property of the installation — every extension ships its own and an installation can override a language file through LANG/resourceOverrides — so search with typo3_label_lookup rather than working from memory.",
      "Correct such a file in place rather than beside it: it keeps its path and it keeps its unit ids, and its wording becomes English. The wording that was there moves into the locale-prefixed file next to it — de.locallang.xlf for a German source — whose file element declares source-language=\"en\" target-language=\"de\" and points original= at the source path, and where each unit repeats the id with the English text as <source> and the wording it replaced as <target>.",
      "Adding an en.-prefixed file is never the correction. The unprefixed file is read as the default locale, and default is the fallback of every other locale, so the non-English wording keeps surfacing everywhere a locale has no translation of its own — while en.locallang.xlf is reached only by the en locale itself. The core ships no en.-prefixed language file outside one test fixture.",
      {
        "text": "A locale-prefixed file whose file element declares no target-language is read as a default-language template: XliffLoader::parseXliff1() decides that on the attribute alone, takes every unit from <source> and discards the <target> wording. Nothing is raised, logged or deprecated, so the symptom is a maintained German file whose labels render in English. XliffParser no longer decides it and carries a deprecation of its own, so reading that class answers a different question.",
        "since": 14
      },
      {
        "text": "No schema check reports this. XLIFF 1.2 requires original, source-language and datatype on the file element and leaves target-language optional, so the file that loses its translations is valid XLIFF. Read the attribute off each locale-prefixed file itself.",
        "since": 14
      },
      "The XLIFF linter symfony/translation ships is what these files are checked with, and a TYPO3 installation already has the package: it validates against the XLIFF schema rather than only parsing the XML. Its requireStrictFileNames constructor argument defaults to true, which is Symfony's own locale-suffix convention — messages.de.xlf — so at the default it rejects every locale-prefixed file with a mismatch between the file name and the target-language attribute. Pass false.",
      "Schema validity is the whole of what that linter establishes. That a translation declares target-language at all, that a source file is authored in English, and that a translation carries no unit its source has dropped are checks the project owns.",
      "Two neighbours: translation-domain for how a label is referenced, label-retirement for one that is no longer used.",
      {
        "text": "A unit a JavaScript module consumes arrives there later than it arrives in PHP — the l10n cache and a year-long browser cache sit between the file and the module. javascript-labels is that subject.",
        "since": 14
      },
      {
        "text": "A backend JavaScript module reads a label with lll() from @typo3/core/lit-helper, and the key is one of window.TYPO3.lang, not an LLL: reference. What fills that object is the file a controller passed to PageRenderer::addInlineLanguageLabelFile(), so a label nobody inlined is missing from it and lll() answers the empty string — no error, and an empty caption where the text was expected.",
        "until": 13
      }
    ]
  },
  {
    "id": "javascript-labels",
    "title": "Labels in a Backend JavaScript Module",
    "domains": [
      "xliff",
      "typescript"
    ],
    "appliesTo": [
      "~labels/",
      "labels module",
      "javascript labels",
      "label in a JavaScript module",
      "Label is not defined",
      "LabelProvider",
      "label-provider",
      "labels.get(",
      "language_domain",
      "JavaScriptLanguageDomainProvider",
      "Build/types/labels/",
      "import labels",
      "new label does not appear",
      "label does not show up",
      "label is missing at runtime",
      "lll("
    ],
    "hints": [
      {
        "text": "A module reaches a label by its translation domain: import labels from '~labels/core.bookmarks', then labels.get('groupType.global') with ICU placeholders. A key the imported bundle does not carry fails at runtime and nowhere earlier — label-provider.ts throws \"Label is not defined: <key>\". Nothing on disk answers that module name either: the backend resolves the ~labels/ import map prefix onto its language_domain route, where JavaScriptLanguageDomainProvider::createLanguageDomainResponse() builds the module per request out of LanguageService::getLabelsFromResource().",
        "since": 14
      },
      {
        "text": "That import is what replaced lll() from @typo3/core/lit-helper, which is still exported and which no module calls any more. So a call to it is a file older than the convention rather than a second way of reading a label.",
        "since": 14
      },
      {
        "text": "So a trans-unit added to the backing XLF is not in the served bundle until the l10n cache is flushed. LocalizationFactory keys the parsed file on the domain, the locale and its fallback chain and on nothing about the file itself — no modification time, no content hash — so the entry written before the unit existed keeps answering. That cache is a SimpleFileBackend in group system: typo3 cache:flush --group=system.",
        "since": 14
      },
      {
        "text": "A browser hard reload is the second half, and it is owed after the flush rather than instead of it. The response carries Cache-Control: private with a max-age of one year, and the URL it was fetched from does not move when a label file changes: the route's cacheBustInfix is the package-dependent cache identifier hashed with JavaScriptLanguageDomain, which is the TYPO3 version, the project path and the installed package list. A flush alone leaves the page on the bundle it already has, which reads as a flush that did not help.",
        "since": 14
      },
      {
        "text": "In the core the build goes green over both steps. grunt scripts runs generate-types:labels, which reads the same XLF files and writes only the type stubs under Build/types/labels/ that Build/tsconfig.json maps ~labels/* onto — so the new key type-checks, the editor completes it and lint passes while the served bundle is still the old one. Those stubs are generated rather than committed, so nothing in a diff shows it either.",
        "since": 14,
        "scope": "core"
      },
      {
        "text": "Two neighbours: translation-domain for the domain the module is named by, page-cache-flushing for what a change of another kind invalidates.",
        "since": 14
      }
    ]
  },
  {
    "id": "translation-domain",
    "title": "Referencing a Label by Its Domain",
    "domains": [
      "xliff"
    ],
    "appliesTo": [
      "translation domain",
      "package.resource",
      "domain of a label",
      "label reference",
      "EXT: file path in a label"
    ],
    "hints": [
      {
        "text": "Reference a label by its translation domain (package.resource:key), not by the EXT: file path. The domain follows from the path and is registered nowhere: Resources/Private/Language/ is dropped, remaining directories become dot-separated snake_case parts, locallang.xlf becomes \"messages\", locallang_<suffix>.xlf becomes \"<suffix>\", any other file name becomes its own snake_case form, and Configuration/Sets/<Set>/labels.xlf becomes \"sets.<set>\".",
        "since": 14
      },
      {
        "text": "Because the domain is derived rather than declared, a file a patch is about to add already has one. Call typo3_translation_domain_lookup with the path instead of guessing; a wrong guess only fails at runtime.",
        "since": 14
      },
      {
        "text": "Two files can derive the same domain, and then the one without the locallang prefix wins — SudoMode.xlf takes sudo_mode from locallang_sudo_mode.xlf beside it. A locale prefix changes nothing: de.locallang.xlf is the translation of locallang.xlf and derives its domain, not one of its own.",
        "since": 14
      }
    ]
  },
  {
    "id": "label-retirement",
    "title": "Retiring a Label Instead of Deleting It",
    "domains": [
      "xliff"
    ],
    "appliesTo": [
      "delete a label",
      "remove a trans-unit",
      "x-unused-since",
      "subState",
      "deprecated label",
      "unused label"
    ],
    "hints": [
      "Never delete a trans-unit. The key outlives the code that stopped using it: third-party code still references it, so does a translation override in an installation, and removing it breaks both silently.",
      {
        "text": "Retire a label instead, by marking its trans-unit x-unused-since=\"<the release it becomes unused in>\" — the upcoming one, not the current one. In XLIFF 2.0 the same thing is subState=\"deprecated\" on the segment. A label marked that way raises an E_USER_DEPRECATED when it is resolved, which is how the remaining usages find their own way to the surface.",
        "since": 14
      },
      "Remove a marked trans-unit only in the breaking-change patch of a later major, together with the other removals. A label that was introduced and dropped again inside the same patch is simply deleted — it was never released, so there is nothing to deprecate.",
      {
        "text": "Marking a label unused is a deprecation and is treated as one: it needs its changelog entry, and every usage inside the core migrates in the same patch. The translator history on translate.typo3.org hangs off the key, which is the other thing a deletion throws away.",
        "scope": "core"
      }
    ]
  },
  {
    "id": "site-label-language",
    "title": "Core Labels on a Non-English Site",
    "domains": [
      "xliff",
      "typoscript",
      "php"
    ],
    "appliesTo": [
      "site language",
      "site configuration",
      "site settings",
      "site set",
      "non-English",
      "German site",
      "core labels",
      "English labels",
      "typo3Language",
      "locale",
      "language pack",
      "language:update",
      "availableLocales",
      "availableLanguages",
      "submitButtonLabel"
    ],
    "hints": [
      "A site's locale and its TYPO3 label language are separate. When typo3Language is absent, SiteLanguage derives it from the locale including its country, so de_DE.UTF-8 becomes de_DE although the German pack is named de. Set typo3Language: de on that site language; a mismatch silently falls back to the English source instead of reporting an error.",
      {
        "text": "Activate the locale before downloading its pack: set LANG/availableLocales to an array containing de, then run typo3 language:update de. The CLI can write the array with typo3 configuration:set LANG/availableLocales '[\"de\"]' --json.",
        "since": 14
      },
      {
        "text": "Activate the language before downloading its pack: set $GLOBALS['TYPO3_CONF_VARS']['EXTCONF']['lang']['availableLanguages'] to an array containing de in config/system/additional.php, then run typo3 language:update de.",
        "until": 13
      },
      "typo3 language:update exits 0 whether or not a pack arrived: a download the server answered with nothing is a warning the progress bar hides, --no-progress prints one line per pack instead, and --fail-on-warnings is what turns a miss into a failure. What says it worked is the labels directory holding the extension's .xlf files under that iso code — var/labels/<iso>/<extension key>/ in a Composer installation — because a pack that never arrived leaves the English labels and looks exactly like the typo3Language mismatch above.",
      "A pack only translates labels that are translation references. EXT:form's standard submit button fallback is the literal Submit; set renderingOptions.submitButtonLabel in the form definition for the site's language."
    ]
  }
]
