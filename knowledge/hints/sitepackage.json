[
  {
    "id": "sitepackage-layout",
    "title": "How a Sitepackage Is Laid Out",
    "domains": [
      "fluid",
      "typoscript"
    ],
    "appliesTo": [
      "sitepackage",
      "site package",
      "theme extension",
      "theme",
      "directory structure",
      "folder structure",
      "how do I lay out"
    ],
    "hints": [
      {
        "text": "The core ships a theme of its own — the package typo3/theme-camino, typo3/sysext/theme_camino in a core checkout, absent from a Composer project that did not require it. It is where the layout below is worked out in full, so read it rather than invent one. Read, not depend on: it is the theme of one release line, announced to move out of the core into a repository of its own, and a project sitepackage that requires it or extends its set buys that migration for nothing.",
        "since": 14
      },
      {
        "text": "The language files are split by who reads them — the frontend messages, the backend field labels, the backend layout titles, the content element previews. Each file is its own translation domain, and the domain is what a label is referenced by, so the split is what makes the references readable.",
        "since": 14
      },
      "All of this is the extension. What sits around it in a project — where the browser tests go, what belongs in Build/, which scripts the repository exposes — is its own subject, because the core has no answer for it: it is not a project. That is the project repository layout hint.",
      "Where the files themselves go is its own hint: sitepackage-templates for the template root, sitepackage-backend-layouts for the page layouts, sitepackage-typoscript-reference for how one object reaches another."
    ]
  },
  {
    "id": "sitepackage-templates",
    "title": "The Fluid Template Root and What Is Addressed From It",
    "domains": [
      "fluid"
    ],
    "appliesTo": [
      "Resources/Private/Templates/",
      "content element template",
      "page template",
      "f:layout name",
      "Layouts/",
      "Partials/",
      "template is not found",
      "templateName"
    ],
    "hints": [
      "One template root carries all of it: Resources/Private/Templates/, with Pages/ for the page templates, Content/ for the content element templates, ContentPreviews/ for their backend previews, Layouts/, and Partials/ grouped in subdirectories. That directory is the single paths entry PAGEVIEW is given, and lib.contentElement is pointed at Templates/Content/ with the same Layouts/ and Partials/ — so the page and its content elements are rendered out of one tree instead of two.",
      "A layout is addressed with its subdirectory: <f:layout name=\"Pages/Default\" /> in a page template, <f:layout name=\"Content/Default\" /> in a content element. The page frame and the content element frame are both called Default, and fluid_styled_content ships a Default of its own, so a shared Layouts/ root without those subdirectories renders the site header and footer inside every single content element — on a page that still comes back without an error.",
      "The partial root is the same shared name space, and the collision there is not confined to the element that causes it: fluid_styled_content ships Partials/Header/Header and renders it from Partials/Header/All, so a partial of that name in a later root renders the header of every core element on the page. It is handed the core's arguments rather than its own, so a required argument the core does not pass turns every page carrying a core element into an exception, where the layout collision rendered the wrong thing without one. Footer/, Media/, Bullets/, Table/ and DropIn/ sit beside it in that root, so an extension adding partialRootPaths puts its own partials under a directory name nothing else uses.",
      "A content element names its own template: fluid_styled_content sets templateName = Default on lib.contentElement and ships no Default template, and every element it renders sets its own beside its rendering definition — Configuration/TypoScript/ContentElement/Text.typoscript is tt_content.text =< lib.contentElement followed by templateName = Text. An element copied off lib.contentElement without one carries Default and asks for a template neither package ships.",
      {
        "text": "Deriving the name from the CType instead is theme_camino's configuration, and a sitepackage inherits that convention only by reproducing it: Configuration/Sets/camino/TypoScript/content.typoscript unsets templateName and sets templateName.ifEmpty.cObject on the CType field with case = uppercamelcase, so camino_textmedia_teaser renders Content/CaminoTextmediaTeaser.fluid.html. The same file points tt_content.default at lib.contentElement, so the derivation answers for every CType instead of for the ones a rendering definition names. Under it a snake_case CType is a requirement rather than a habit — words the identifier does not separate the file name does not separate either.",
        "since": 14
      }
    ]
  },
  {
    "id": "sitepackage-backend-layouts",
    "title": "Where a Page Layout Is Declared",
    "domains": [
      "typoscript",
      "fluid"
    ],
    "appliesTo": [
      "Configuration/BackendLayouts/",
      "backend layout",
      "page layout",
      "mod.web_layout.BackendLayouts",
      "BackendLayouts"
    ],
    "hints": [
      "One page layout per file, and one name for it: the identifier the file declares below mod.web_layout.BackendLayouts is also what the page template is named after, so the file name, the identifier and Pages/<Name>.fluid.html are one name written in three places.",
      {
        "text": "Where those files live follows from one question — does anything reach a backend without the set? A project sitepackage ships one set and no Configuration/page.tsconfig of its own, so nothing does: the layouts belong in Configuration/Sets/<Set>/BackendLayouts/, pulled in with @import './BackendLayouts/*.tsconfig' from the set's page.tsconfig, which is the same place and the same form the TypoScript split already has. Configuration/BackendLayouts/ at extension level is for the other case — several sets, or a distributable theme like theme_camino, where the extension is the unit and an extension-level page.tsconfig carries what has to apply whether a site depends on the set or not. Where the extension has no such file, moving the layouts up there is an indirection with no effect. The directory keeps its name either way, because the name says what is in it: page TSconfig that is not a backend layout goes into page.tsconfig or a sibling directory, not in there.",
        "since": 13
      }
    ]
  },
  {
    "id": "sitepackage-typoscript-reference",
    "title": "Referencing a TypoScript Object Instead of Copying It",
    "domains": [
      "typoscript"
    ],
    "appliesTo": [
      "=<",
      "reference operator",
      "copy operator",
      "lib.fluidPage",
      "data processor is missing",
      "import order"
    ],
    "hints": [
      "page.<n> =< lib.fluidPage is a reference and not a copy: it is resolved while the page renders, out of the merged setup, so a data processor added to lib.fluidPage in a file that is imported later still arrives. The copy operator < is the one where the order of the imports decides what the object ends up holding."
    ]
  },
  {
    "id": "sitepackage-fluid-styled-content",
    "title": "What a Sitepackage Takes From fluid_styled_content",
    "domains": [
      "typoscript",
      "fluid"
    ],
    "appliesTo": [
      "fluid_styled_content",
      "fluid-styled-content",
      "lib.contentElement",
      "fluid-styled-content-css",
      "depend on fluid styled content",
      "do I need fluid_styled_content",
      "own content element rendering",
      "_CSS_DEFAULT_STYLE",
      "remove fluid_styled_content",
      "drop fluid_styled_content",
      "sitepackage without fluid_styled_content",
      "templateName = Generic",
      "tt_content.shortcut",
      "styles.templates"
    ],
    "hints": [
      "The object a sitepackage takes the dependency for is lib.contentElement: a FLUIDTEMPLATE with templateName Default, the extension's own Templates, Partials and Layouts first and the styles.templates.* constants above them, and the settings the shipped templates read — the header type, the image lazy loading and decoding, the lightbox wrap. An element written as tt_content.<CType> =< lib.contentElement copies all of that and then overrides the template name.",
      {
        "text": "It also carries a dataProcessing entry of its own, record-transformation, so an element copying it is handed the transformed record rather than the raw row.",
        "since": 14
      },
      "lib.parseFunc and lib.parseFunc_RTE are not its. EXT:frontend declares both in its own ext_localconf.php, so rich text rendering is there whether or not this extension is required.",
      "The typo3/fluid-styled-content-css set adds one thing: plugin.tx_frontend._CSS_DEFAULT_STYLE with the .ce-* rules that style the elements this extension renders. A site whose page TSconfig removes every one of those elements from the selector is shipping stylesheet rules for markup it never emits.",
      "Defining lib.contentElement in the sitepackage instead is a FLUIDTEMPLATE of a few lines and drops the dependency, and what goes with it is the rendering of the core's own elements — text, textmedia, header, table, the menus. That is the decision to make deliberately: a site that keeps a few of them keeps the dependency, and one that offers only its own elements is paying for a scaffold it overrode.",
      "Every Extbase plugin renders through this extension as well. ExtensionUtility::configurePlugin() writes tt_content.<plugin signature> =< lib.contentElement with templateName = Generic, and Generic is one of the templates this extension ships: it renders {content} where the plugin assigned it and otherwise an f:cObject back onto the plugin's own tt_content path, which is where the same call put the EXTBASEPLUGIN. EXT:form, EXT:felogin and EXT:indexed_search each register a plugin that way.",
      "Dropping the dependency without putting lib.contentElement back leaves every one of those plugins silent. tt_content is a CASE on the CType and the plugin's own key is still written, so the yellow \"has no rendering definition\" default never fires and the element renders as nothing. Putting lib.contentElement back without a Generic template beside it fails the other way, with InvalidTemplateResourceException out of FLUIDTEMPLATE naming the template it looked for and every root it checked.",
      {
        "text": "A plugin registered with the list subtype takes a second path through this extension: tt_content.list is one of its content element files and sets templateName = List.",
        "until": 13
      },
      "The core's own elements are split across three extensions, and only the last part is this one's. EXT:frontend registers the CType and its TCA, EXT:backend draws the page module preview in StandardContentPreviewRenderer, and this extension holds the frontend rendering — Configuration/TypoScript/ContentElement/<Name>.typoscript and the template beside it. So dropping it leaves every core element still selectable and still previewed, and empty on the page. shortcut is the one to check first, because a site offering only its own elements tends to keep that one.",
      "The Default layout renders Before, Header, Footer and After as optional sections whose fallback child is a partial — DropIn/Before/All, Header/All, Footer/All and DropIn/After/All. An element declaring none of those sections still renders those partials, so a layout written to replace this one owes the partials too or drops the sections deliberately.",
      "The styles.templates.* and styles.content.* settings the shipped objects read are declared by this extension and by no other. A constant nothing declares is left as its own text — ConstantAwareTokenStream::__toString() falls back to the token — so a reference surviving the removal puts the literal {$styles...} into the value instead of emptying it."
    ]
  }
]
