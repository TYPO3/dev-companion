[
  {
    "id": "deprecation",
    "title": "Deprecation",
    "skill": "typo3-extension-upgrade",
    "skillCore": "typo3-core-patch-development",
    "match": [
      "deprecat"
    ],
    "rulesQuery": "deprecation changelog",
    "checklist": [
      "Annotate the member @deprecated since TYPO3 v<the version this lands in>, will be removed in TYPO3 v<the next major>. and close that line with the migration sentence. Read both versions off the branch you are on rather than off an example.",
      "Keep the member working and open its body with trigger_error('<Class>-><member>() is deprecated since TYPO3 v<the version this lands in> and will be removed in TYPO3 v<the next major>. <migration>', E_USER_DEPRECATED). The docblock and the message say the same three things, and the removal is a patch of its own in that major.",
      "Migrate every caller of the deprecated API in the same patch, and confirm none is left behind.",
      "Add a changelog file below typo3/sysext/core/Documentation/Changelog/<the minor this is released in>/ named Deprecation-<issue>-<UpperCamelCaseDescription>.rst. Nothing has to include it: that directory's Index.rst pulls Deprecation-* in by glob, so the filename is the inclusion.",
      "Open the changelog file with .. include:: /Includes.rst.txt and a unique .. _deprecation-<issue>-<unix timestamp>: anchor directly above the 'Deprecation: #<issue> - <title>' headline, with See :issue:`<issue>` under it. Then Description, Impact, Affected installations and Migration — the last two are what this type owes over a feature entry.",
      "End the changelog file with .. index:: carrying at least one subject tag and exactly one of FullyScanned, PartiallyScanned or NotScanned. Build/Scripts/validateRstFiles.php rejects a Deprecation file without the scanner tag, so it is owed rather than considered.",
      "Back a FullyScanned or PartiallyScanned tag with an extension scanner matcher: an entry below typo3/sysext/install/Configuration/ExtensionScanner/Php/, keyed by the deprecated symbol and naming the changelog file in its restFiles. NotScanned is for what no matcher can find, not for what nobody wrote.",
      "Use [TASK] or [FEATURE] as the commit keyword. A deprecation must never use the [!!!] breaking prefix."
    ],
    "checks": [
      "CI=true ./Build/Scripts/runTests.sh -s checkRst",
      "CI=true ./Build/Scripts/runTests.sh -s checkExtensionScannerRst"
    ],
    "tools": [
      "typo3_commit_message_guide with workflow=\"core\" and isDeprecation=true, for the keyword and prefix rules a message pushed to Gerrit is held to"
    ]
  },
  {
    "id": "breaking",
    "title": "Breaking change",
    "skill": "typo3-extension-upgrade",
    "skillCore": "typo3-core-patch-development",
    "match": [
      "breaking",
      "remove public",
      "removed public",
      "drop support",
      "@internal",
      "is this breaking",
      "public api"
    ],
    "rulesQuery": "breaking change changelog",
    "checklist": [
      "Settle first that the change is breaking at all: @internal says the API is not public and does not decide it, and what does is whether anything outside the core calls it. The core has removed @internal members both ways. A change that moves no member is settled on what it renders instead, which typo3_hint_lookup with the id breaking-without-a-moved-member states.",
      "Add a changelog file below typo3/sysext/core/Documentation/Changelog/<version>/ named Breaking-<issue>-<Title>.rst.",
      "Prefix the commit summary with [!!!] before the keyword.",
      "Target main; confirm any older release target with the release managers.",
      "Describe the migration path for extension authors.",
      "Add an extension scanner matcher entry below typo3/sysext/install/Configuration/ExtensionScanner/Php/ for every method, property and class the changelog entry removes, with restFiles naming that entry. Which file takes which kind of removal is in the Breaking Changes section typo3_rule_lookup answers with.",
      "Tag the changelog entry NotScanned, PartiallyScanned or FullyScanned for what those matchers actually reach."
    ],
    "checks": [
      "CI=true ./Build/Scripts/runTests.sh -s checkRst",
      "CI=true ./Build/Scripts/runTests.sh -s checkExtensionScannerRst"
    ],
    "tools": [
      "typo3_commit_message_guide with workflow=\"core\" and isBreaking=true, for the message a change pushed to Gerrit is held to"
    ]
  },
  {
    "id": "compatibility",
    "title": "Keeping a package running on the majors it declares",
    "skill": "typo3-extension-upgrade",
    "guide": "extension/compatibility/running-on-a-declared-major-that-is-not-installed",
    "match": [
      "compatible with",
      "compatibility",
      "package runs on",
      "package run on",
      "extension runs on",
      "extension run on",
      "still work on",
      "still works on",
      "supports typo3",
      "support typo3"
    ],
    "matchWeak": [
      "compatible",
      "runs on",
      "run on",
      "support for"
    ],
    "condition": "only if the work is what a package owes the TYPO3 majors it declares — adding one, dropping one, or proving it runs on each — rather than carrying one installation from the major it is on to the next",
    "rulesQuery": "",
    "checklist": [
      "Read the range the package declares before naming a target. The Composer manifest and ext_emconf.php both declare it and a difference between the two is itself a finding, because for a non-Composer installation ext_emconf.php is the only constraint that governs.",
      "The installed major is one point in that range. A judgement made against it alone holds for that point, and every other declared major is a question about a branch this installation does not have.",
      "Ask it per symbol rather than per package. A class that stands on both branches can have gained a method on the newer one, a return type can be nullable on one side, and a constant can hold a different set of entries.",
      "The lowest declared major decides every shape in the package. A registration form, attribute or API introduced later cannot replace one that still has to work there, so a runtime branch on the version is what the declared range requires rather than debt to clean up.",
      "Prove every combination the declaration claims rather than the one the installation supplies. A cell that will not resolve is the finding, and a cell nobody ran is named as unrun — the matrix is the claim the package makes about itself.",
      "Dropping a major is the user's decision and never one taken to make the code simpler. Widening to a new one keeps every version currently declared unless the request says otherwise."
    ],
    "checks": [],
    "tools": [
      "typo3_project_describe, for the range the package declares against the version the installation supplies — the pair every judgement here is made about",
      "typo3_changelog_lookup with type=\"breaking\" or \"deprecation\" and the target version, for what the package's own call sites owe it",
      "typo3_system_extension_lookup, for whether a system extension the package requires is still part of the target major",
      "typo3_documentation_lookup with the target version, where the official API decides the shape and the installed core cannot answer for it"
    ]
  },
  {
    "id": "changelog",
    "title": "Changelog entry",
    "skill": "typo3-extension-documentation",
    "skillCore": "typo3-core-patch-development",
    "match": [
      "changelog",
      "rst",
      "release note"
    ],
    "rulesQuery": "changelog rst documentation",
    "checklist": [
      "Changelog files live below typo3/sysext/core/Documentation/Changelog/, in the directory of the minor version the change is released in; a backport goes into the <lts>.x directory of the oldest branch it reaches. The file is named <Type>-<forgeIssueNumber>-<UpperCamelCaseDescription>.rst.",
      "The type is the first of four that describes the change: Breaking where it moves or removes core functionality third-party code may use, Deprecation where it marks core functionality for a planned removal, Feature where it adds functionality, Important for anything else that may require manual action and the only one an LTS release may carry. A casual bug fix owes no entry, and which fix is casual is settled by typo3_rule_lookup with documentId=core/contribution/changelog rather than by this item. There is no Task type; checkRst fails a title opening with anything else.",
      "Do not assemble the skeleton by hand: the core ships Build/rstTemplates/rstTemplate<Type>.rst per type, and ./Build/Scripts/runTests.sh -s watchRst core interactive writes the file from it and renders it while you edit. It writes into the directory of the release that branch develops, so a backport's file is moved afterwards."
    ],
    "checks": [
      "CI=true ./Build/Scripts/runTests.sh -s checkRst"
    ],
    "tools": [
      "typo3_rule_lookup with documentId=core/contribution/changelog, for the whole procedure: the type, the name and the checks of a file below typo3/sysext/core/Documentation/Changelog/",
      "typo3_hint_lookup with id=documentation-changelog, for the skeleton such a file is written from in a core checkout, and the tags it ends on"
    ]
  },
  {
    "id": "documentation",
    "title": "Documenting a package for its readers",
    "skill": "typo3-extension-documentation",
    "skillCore": "typo3-core-patch-development",
    "guide": "extension/documentation/manual",
    "match": [
      "documentation",
      "document the",
      "document our",
      "document this",
      "document how",
      "documenting the",
      "documenting our",
      "readme"
    ],
    "rulesQuery": "",
    "checklist": [
      "Document what the checkout implements rather than what the task described. A configuration key, a ViewHelper argument, a label or a command that is documented and not registered is a defect nobody finds until a reader follows it, and typo3_extension_describe reports what the package registers and whether it has a manual and a README at all.",
      "Write it where that package keeps its documentation. An extension's manual is Documentation/ inside the extension, with Index.rst as its entry point and guides.xml as the renderer configuration; the README of the project around it documents the project, so a workflow written there is documentation the package's own readers never receive.",
      "Separate what an integrator installs and configures from what an editor does in the backend. One page for both is where the setup steps end up in front of the editors.",
      "Document the public surface and leave the rest out. A controller, a service or a method the package never promised is implementation detail, and describing it as behaviour is a promise the next release has to keep.",
      "Bind a version claim to what the package declares rather than to the installation it was read in. The Composer constraint says which majors it serves, and a claim about TYPO3 itself is answered by typo3_documentation_lookup at the version being documented."
    ],
    "checks": [],
    "tools": [
      "typo3_extension_describe, for what the package registers, whether it has a manual and a README, and which XLF files declare its labels",
      "typo3_hint_lookup with id=extension-documentation, for where an extension's manual lives and what a release makes public",
      "typo3_documentation_lookup with the target version, for the TYPO3 API or workflow a claim rests on"
    ]
  },
  {
    "id": "labels",
    "title": "User-facing labels",
    "match": [
      "xlf",
      "xliff",
      "trans-unit",
      "locallang",
      "translation",
      "language file"
    ],
    "matchWeak": [
      "label",
      "wording"
    ],
    "condition": "only if the change adds, edits, or retires an XLF trans-unit",
    "rulesQuery": "",
    "checklist": [
      "Reference labels by their translation domain (package.resource:key), not by the EXT: file path. typo3_translation_domain_lookup computes the domain of any XLF path.",
      "Never delete a trans-unit: mark it x-unused-since=\"<next version>\" and remove it in a later major.",
      "Write the source unit in English. A translation maintained in the package belongs in the locale-prefixed XLF beside that source file.",
      "Correct a source file that is not English in place: same path, same unit ids, English wording, and the wording it replaced moved into the locale-prefixed file beside it as <target> under source-language=\"en\" target-language=\"<locale>\". Adding an en.-prefixed file is not the correction — the unprefixed file is the default locale every other locale falls back to."
    ],
    "checks": [
      "CI=true ./Build/Scripts/runTests.sh -s checkIntegrityXliff",
      "CI=true ./Build/Scripts/runTests.sh -s normalizeXliff"
    ],
    "tools": [
      "typo3_translation_domain_lookup, for the domain an XLF file resolves to"
    ]
  },
  {
    "id": "backend-module",
    "title": "Adding or changing a backend module",
    "skill": "typo3-backend-module-development",
    "skillCore": "typo3-core-patch-development",
    "match": [
      "backend module",
      "module registration",
      "Configuration/Backend/Modules.php",
      "module identifier",
      "doc header",
      "docheader"
    ],
    "rulesQuery": "",
    "checklist": [
      "Settle where the module sits before it is named: which module it hangs under, where it stands among that parent's children, and who may open it. The identifier is what the route, the module's persisted per-user state and a user's bookmarks are keyed by, so a later rename is an alias kept beside the module rather than an edit to it.",
      "Read the modules this installation already registers before choosing that place. A parent that is not there and an identifier that is taken are written the same way as a working one, and both fail when a user opens the module rather than when the file is saved.",
      "Answer who may open the module and what a request inside it may read as two questions. The access declared on the registration decides the first — the entry in the tree, and the route behind it — and says nothing about the second, so a module showing records somebody else owns checks that itself.",
      "Prove the module by opening it: the entry appears where it was placed, the route answers, the icon renders instead of an empty box, the labels resolve instead of showing their keys, and the access declared lets in whom it claims to. None of the five fails at build time, and a green test suite reports none of them."
    ],
    "checks": [],
    "tools": [
      "typo3_backend_module_lookup, before choosing the identifier, the parent or the route: it reports what this installation already registers",
      "typo3_hint_lookup with id=backend-modules, for the registration file, what a module builds into its doc header, and what its own POST answers with",
      "typo3_translation_domain_lookup, for the XLF the module's labels are declared through"
    ]
  },
  {
    "id": "backend-ui",
    "title": "Backend UI markup",
    "match": [
      "markup",
      "css class",
      "component",
      "badge",
      "card",
      "button",
      "modal"
    ],
    "matchWeak": [
      "template",
      "fluid",
      "backend module"
    ],
    "condition": "only if the change adds or alters backend component markup or CSS classes",
    "rulesQuery": "backend ui component",
    "checklist": [
      "Use the existing backend component classes and their documented markup instead of new ad-hoc classes.",
      "Check the styleguide demo of the component for the canonical structure."
    ],
    "checks": [
      "CI=true ./Build/Scripts/runTests.sh -s lintScss",
      "CI=true ./Build/Scripts/runTests.sh -s build"
    ],
    "tools": [
      "typo3_component_lookup, before writing backend markup or CSS classes"
    ]
  },
  {
    "id": "icons",
    "title": "Icons",
    "match": [
      "icon"
    ],
    "rulesQuery": "icon",
    "checklist": [
      "Resolve the icon identifier in your own checkout before using it: IconRegistry is built from every installed package, so an identifier that exists in one instance may not exist in another, and an unknown one only fails at runtime."
    ],
    "checks": [],
    "tools": []
  },
  {
    "id": "tests",
    "title": "Test coverage",
    "skill": "typo3-extension-testing",
    "skillCore": "typo3-core-patch-development",
    "guide": "extension/testing/phpunit",
    "match": [
      "test",
      "coverage",
      "regression"
    ],
    "rulesQuery": "testing strategy",
    "checklist": [
      "Confirm the test fails without the change and passes with it.",
      "Run the single test file or method while iterating instead of the whole suite."
    ],
    "checks": [],
    "tools": [
      "typo3_test_run_guide, for the targeted runTests.sh invocation",
      "typo3_hint_lookup id=project-extension-tests, where the extension is not part of the core and the suite has to be assembled first"
    ]
  },
  {
    "id": "browser-tests",
    "title": "Browser tests",
    "skill": "typo3-extension-testing",
    "skillCore": "typo3-core-patch-development",
    "guide": "project/testing/playwright",
    "match": [
      "playwright",
      "browser test",
      "e2e",
      "end-to-end",
      "end to end",
      "spec.ts",
      "acceptance test",
      "codeception",
      "visual regression"
    ],
    "rulesQuery": "browser tests playwright",
    "checklist": [
      "Read skills/typo3-extension-testing/references/playwright.md before writing the first spec. It is the implementation guide for this layer and this brief does not repeat what it carries.",
      "Keep the suite with what is deployed rather than with the package: a spec needs a served site and a real URL, which an extension package alone does not have."
    ],
    "checks": [],
    "tools": [
      "typo3_hint_lookup id=browser-tests, for the layout the core's own suite established and what a backend spec has to locate through",
      "typo3_hint_lookup id=browser-tests-outside-core, for the site a suite outside the core runs against"
    ]
  },
  {
    "id": "browser-check",
    "title": "Looking at a change in a browser",
    "guide": "any/testing/browser-check",
    "guideCore": "any/testing/browser-check",
    "changesNothing": true,
    "match": [
      "in the browser",
      "in a browser",
      "real browser",
      "browser check",
      "screenshot",
      "visually",
      "frontend verification",
      "verify the frontend",
      "look at the rendered",
      "look at the page",
      "open the site",
      "open the page",
      "renders correctly",
      "see it render",
      "confirm it renders"
    ],
    "rulesQuery": "",
    "checklist": [
      "Looking is what settles a change nothing asserts, so it is owed rather than offered: a view nobody opened is reported as unverified, in those words, rather than as a limit of what was available.",
      "Seeing it once is not coverage. Where the behaviour has to keep holding, the spec that asserts it is the step after this one."
    ],
    "checks": [],
    "tools": [
      "typo3_project_describe, for the sites this installation serves and the base each answers on, which is what a browser is pointed at"
    ]
  },
  {
    "id": "coding-standards",
    "title": "Coding standards and the code style fixer",
    "skill": "typo3-extension-testing",
    "skillCore": "typo3-core-patch-development",
    "match": [
      "coding standard",
      "coding standards",
      "coding guidelines",
      "code style",
      "php-cs-fixer",
      "cs fixer",
      "code formatter",
      "code formatting",
      "editorconfig",
      "cgl"
    ],
    "matchWeak": [
      "formatting",
      "fixer",
      "reformat"
    ],
    "condition": "only if the change establishes or runs a code style check rather than editing code the check happens to cover",
    "rulesQuery": "",
    "checklist": [
      "Read what the repository already has before adding anything: a .php-cs-fixer.dist.php, an .editorconfig, a Composer script naming either, and a CI step calling one. Half an infrastructure is the ordinary case, and a second rule set beside an existing one is a second answer to maintain.",
      "Take the rule set from typo3/coding-standards rather than assembling one, and let the solver pick the release: the package is below 1.0 and its PHP floor decides which one the extension's declared range can have.",
      "Keep the check and the fix as two commands. The fixing run exits 0 whether it rewrote the tree or not, so only the dry run is a verdict CI can read.",
      "Confirm what the fixer's paths actually reach with its own dry run before the first write, and read the file list rather than the summary. The shipped extension configuration scans the whole package directory, and what an installed TYPO3 beneath it costs depends on where that installation was put.",
      "Where the tree does not pass the check yet, the conformance commits come first and the commit adding the check comes last, so no commit fails the check it introduces. Verify by running the check at the new HEAD."
    ],
    "checks": [],
    "tools": [
      "typo3_hint_lookup with id=extension-coding-standards, for what typo3/coding-standards ships, what its setup command writes, what the configuration already excludes and which run is the check",
      "typo3_hint_lookup with id=extension-static-analysis, for the analyser half of the same layer",
      "typo3_project_describe, for the TYPO3 and PHP range the package declares and the commands it already has"
    ]
  },
  {
    "id": "reporting",
    "title": "Reporting a core issue",
    "guideCore": "core/contribution/reporting-an-issue",
    "changesNothing": true,
    "scope": "core",
    "condition": "if the report is filed on forge.typo3.org",
    "match": [
      "report a bug",
      "report an issue",
      "file a bug",
      "file an issue",
      "new forge issue",
      "issue title",
      "issue description",
      "create an issue",
      "open an issue"
    ],
    "matchWeak": [
      "forge issue",
      "reporting"
    ],
    "rulesQuery": "",
    "checklist": [
      "File it on the TYPO3 Core project at forge.typo3.org under tracker Bug, and fill TYPO3 Version with the major it was seen on. That is the one field a Bug does not go without; PHP Version, Complexity, Is Regression, Sprint Focus and Tags are optional and most reports leave them empty.",
      "The description renders Textile and not Markdown: h2. for a heading, # and * for the two kinds of list, @Foo::bar()@ for an identifier, and <pre><code class=\"php\"> for a block. Three backticks render as three backticks, and a diff pasted as plain text is read as markup and rewritten.",
      "Take the Category value from the project's own list rather than from memory. It is administered on the tracker and a name recalled from another project is a field nobody can file under.",
      "Leave Target version empty. It says which release a fix is scheduled for, which is settled when somebody takes the fix on and not when the defect is described."
    ],
    "checks": [],
    "tools": [
      "typo3_rule_lookup with documentId=core/contribution/reporting-an-issue, for the fields, the markup and the order the description is written in",
      "typo3_forge_lookup with category=\"*\" for the areas the Category field takes, in the tracker's own spelling",
      "typo3_forge_lookup with query in the words of the symptom, for whether somebody has reported it already"
    ]
  },
  {
    "id": "submission",
    "title": "Patch submission",
    "scope": "core",
    "condition": "if the patch goes to the TYPO3 core itself",
    "match": [
      "gerrit",
      "push",
      "patch set",
      "submit",
      "amend",
      "backport"
    ],
    "rulesQuery": "gerrit push patch set",
    "checklist": [
      "One patch is one commit: improve it by amending, never by adding a second commit.",
      "Push with git push origin HEAD:refs/for/<branch> and keep the Change-Id line untouched."
    ],
    "checks": [
      "CI=true ./Build/Scripts/runTests.sh -s cgl -n"
    ],
    "tools": [
      "typo3_commit_message_guide with workflow=\"core\", to check the message before pushing"
    ]
  },
  {
    "id": "patch-checkout",
    "title": "Fetching a change from review",
    "skillCore": "typo3-core-patch-checkout",
    "changesNothing": true,
    "scope": "core",
    "condition": "if the patch is one somebody pushed to review.typo3.org",
    "match": [
      "patch from review",
      "change from review",
      "pull down",
      "check out the patch",
      "checkout the patch",
      "fetch the patch",
      "cherry-pick",
      "cherry pick",
      "apply the patch",
      "try out the patch",
      "still applies",
      "amend",
      "extend the patch"
    ],
    "matchWeak": [
      "review.typo3.org",
      "refs/changes",
      "patch set"
    ],
    "rulesQuery": "gerrit push patch set",
    "checklist": [
      "Fetch the patch set the change is on rather than the one a link happens to name. A change is a series of patch sets, and reading an older one is a review of code nobody is proposing.",
      "A patch that no longer applies is a finding: the branch moved under a change nobody rebased. Resolving your way to something that compiles destroys that finding and produces a patch its author never wrote, so a conflict you cannot take from the change itself ends the work with what it found.",
      "Leave the checkout on a clean current branch when you are done or when the patch will not apply. A fetched change sitting on a detached head is the state the next session cannot tell from its own work, and a branch you carried the patch onto is that state under a name: delete it as deliberately as you made it. Where the work continues on that branch the checkout stays there and the answer says so, because the branch is the result.",
      "Local work carried onto somebody else's patch set goes up as a further patch set on their change, which is ordinary practice asked for before it is done. The amend leaves them the author and makes you the committer, so nothing about the name has to be corrected; what the diff between two patch sets cannot say is why, so every decision taken on their behalf is said in a comment on the change."
    ],
    "checks": [],
    "tools": [
      "typo3_gerrit_lookup with change=<number>, for the patch set the change is actually on and the commit to hold the checkout against"
    ]
  },
  {
    "id": "triage",
    "title": "Issue triage",
    "skillCore": "typo3-core-issue-triage",
    "changesNothing": true,
    "scope": "core",
    "condition": "if the report is one on forge.typo3.org",
    "match": [
      "triage",
      "backlog",
      "forge.typo3.org",
      "from forge",
      "off forge",
      "in forge",
      "on forge",
      "issue tracker",
      "bug report",
      "issue report",
      "open issues",
      "oldest issues",
      "still reproduce",
      "still happens",
      "still a thing",
      "worth taking on",
      "already fixed",
      "close the issue",
      "old issue",
      "old forge issue",
      "forge issues"
    ],
    "matchWeak": [
      "forge",
      "reproduce",
      "unresolved"
    ],
    "rulesQuery": "",
    "checklist": [
      "A report is about the version it was filed against and never about the one in front of you. Establish what it still claims in the checkout you are standing in, and say which branch that was.",
      "Read the comments before the description. The description is what the reporter saw; whether the report was accepted, reverted or reclassified is in the journal, and on a long-lived issue the note that settles it is regularly the last of them.",
      "A relation marked precedes or duplicates is usually where a previous attempt was filed. What that attempt cost is what answers \"what would this cost\", and it is one read rather than a guess.",
      "A verdict names what would change it: the branch it was established on, the file it was established in, and what a maintainer would need before the issue can move. \"Cannot be settled without X\" is a result and ends the work honestly."
    ],
    "checks": [],
    "tools": [
      "typo3_forge_lookup with open=\"oldest\" or open=\"stale\" to find candidates, then issue=<number> to read one whole; notes=\"people\" drops the patch-set pings when reading many",
      "typo3_gerrit_lookup for the changes the issue answer's reviews field names, which is where an abandoned attempt says why",
      "typo3_gerrit_lookup with commit=<hash> where the checkout has already produced one, for the change it belongs to, the backports sharing its Change-Id and the branches its Releases: trailer claims — which is what an issue is closed as fixed on"
    ]
  },
  {
    "id": "patch",
    "title": "Writing the change",
    "skillCore": "typo3-core-patch-development",
    "match": [
      "fix it",
      "fix this",
      "fix them",
      "fix a",
      "fix an",
      "fix the",
      "work it off",
      "work them off",
      "write the patch",
      "write a patch",
      "implement what"
    ],
    "rulesQuery": "",
    "checklist": [],
    "checks": [],
    "tools": []
  },
  {
    "id": "audit",
    "title": "Review or audit",
    "skill": "typo3-extension-health",
    "skillCore": "typo3-core-patch-review",
    "changesNothing": true,
    "match": [
      "audit",
      "conformance",
      "code review",
      "review the",
      "review this",
      "review of",
      "review patch",
      "review core patch",
      "review change",
      "review only",
      "security review",
      "reviewing the",
      "reviewing this",
      "reviewing my"
    ],
    "rulesQuery": "",
    "checklist": [
      "Where the review is of a change, enumerate what it removes or renames before judging it: the public classes, methods and properties it takes away, and the signature of anything that stays. A task text names what a change is about and never what it takes away, so this is the one surface no other item here can state for you.",
      "Hold each removal in a core patch to what it owes, in the diff rather than in the summary: a matcher entry below typo3/sysext/install/Configuration/ExtensionScanner/Php/ keyed by the removed symbol, a Breaking or Deprecation changelog file its restFiles names, and the [!!!] prefix where the change is breaking. @internal on the member does not settle whether it is — whether anything outside the core calls it does, and the core has removed @internal members both ways.",
      "Run CI=true ./Build/Scripts/runTests.sh -s checkRst and CI=true ./Build/Scripts/runTests.sh -s checkExtensionScannerRst over a core diff that removes something. The second holds a matcher to the changelog file it names and nothing holds a changelog to the matcher it lacks, so a missing matcher is found by reading the removals against the matcher files.",
      "A finding names the location, what the code there does, and the rule or documentation it contradicts. Short of those three it is a question rather than a violation, and reporting it as one costs the maintainer exactly the reading the review skipped.",
      "Run the checks this repository declares rather than recommending them, and read a green result as what its commands cover: an entry no command covers is a gap in the check layer, which is a finding of its own.",
      "Hand each finding over with its consequence and what would remediate it, and route the work to whoever does it. A review changes nothing itself, so the report is what it produces."
    ],
    "checks": [],
    "tools": [
      "typo3_project_describe, for the TYPO3 and PHP versions this repository supports and the commands it declares",
      "typo3_extension_describe, for what each extension in scope registers",
      "typo3_configuration_lookup, where a finding turns on a configuration default: what this installation resolved after every extension has had its say, which the shipped DefaultConfiguration.php cannot state. A path names a subtree as readily as a leaf, so which writers LOG/writerConfiguration holds is one call."
    ]
  },
  {
    "id": "diagnosis",
    "title": "Finding the cause of a defect",
    "changesNothing": true,
    "match": [
      "cause",
      "causing",
      "why",
      "diagnose",
      "diagnosis"
    ],
    "rulesQuery": "",
    "checklist": [
      "Read what the installation wrote down before what it rendered. The page a symptom is reported from is the weakest of the readings there are: typo3_hint_lookup with id=installation-exception-output says where the log is, which exceptions are never written to it at all, and what decides whether the page carries the message.",
      "An empty log is a finding rather than a missing file. It separates something that was thrown from a response TYPO3 returned on purpose, and the two are read in different places — so name what decided it instead of reporting the installation as having nothing to say.",
      "Place the symptom in a subsystem before opening a file, with typo3_hint_lookup on the words it was reported in. What that subsystem's conventions say about where such behaviour can come from is what turns one guess into a list of candidates.",
      "Name the workflow that owns the cause once it is placed. An installation answering wrong is typo3-development-installation's — its site configuration, its boot, the base it answers on. A cause in this package's own code has no workflow before the fix is agreed, and agreeing it is the next task rather than a step of this one."
    ],
    "checks": [],
    "tools": [
      "typo3_hint_lookup, twice: with id=installation-exception-output for where an uncaught exception is written, which codes are only ever displayed and what decides whether the page carries the message, then with the symptom in the words it was reported in for the subsystem that owns it — the placement the rest of the diagnosis stands on",
      "typo3_configuration_lookup, for what this installation has configured where the shipped default says otherwise",
      "typo3_changelog_lookup with type=\"breaking\", where the symptom arrived with an upgrade rather than with a change of your own"
    ]
  },
  {
    "id": "cleanup",
    "title": "Putting a repository right",
    "skill": "typo3-extension-health",
    "match": [
      "put it right",
      "put the repository right",
      "clean up",
      "cleanup",
      "tidy up",
      "fix what is wrong",
      "fix the findings",
      "work off the findings",
      "code quality",
      "modernize",
      "modernise",
      "bring it back into line"
    ],
    "matchWeak": [
      "improve",
      "better",
      "tidy"
    ],
    "condition": "only if the task asks for the repository as a whole to be changed rather than reviewed, or for the findings of a review to be worked off",
    "rulesQuery": "",
    "checklist": [
      "Run the audit before writing the list, and let it own the findings: what a surface is, what evidence a finding rests on, what it is worth and who fixes it are the audit's answers. A list built from a reading of the checkout instead is an impression, and the items in it are not the ones the report would have given.",
      "Show the list whole and let the maintainer cut items, reorder them or stop, before a single file is changed. That agreement is the one step nothing downstream recovers, and a list arriving with the changes it produced is one nobody had the chance to disagree with.",
      "Keep the list in the reply rather than committing it into the repository. A worklist committed into somebody's history is a file nobody asked for that has to be taken out again; what the history keeps is the commits the items produced, each saying which item it closed.",
      "Work an item in the workflow that owns it, and stop before editing files another owner has. An item no workflow owns is worked here only where the project's own suite, linter or static analysis proves the change — anything else goes back unassigned, because a finding nobody owns and no check covers is a hole in the workflow map and quietly filling it hides the hole.",
      "Re-run the audit on the worked list rather than grading it off the diff. Work that declares its own findings gone has no evidence for it, and a file that reads correctly can still be rewritten by the environment that owns it.",
      "Report the items still open, the ones dropped with what dropped them, and the ones sent back unassigned. A finished list and an abandoned one read alike in a summary."
    ],
    "checks": [],
    "tools": [
      "typo3_project_describe, for what the repository is before anything in it is changed",
      "typo3_extension_describe, for what each extension in scope registers",
      "typo3_commit_message_guide with workflow=\"project\", for the message each item's commit carries"
    ]
  },
  {
    "id": "patch-review",
    "title": "Judging one incoming change",
    "skill": "typo3-extension-patch-review",
    "guide": "extension/compatibility/a-declared-major-that-is-not-installed",
    "changesNothing": true,
    "match": [
      "pull request",
      "merge request",
      "review the pr",
      "review this pr",
      "incoming change",
      "proposed change",
      "somebody proposed",
      "review the branch"
    ],
    "matchWeak": [
      "the patch",
      "contribution"
    ],
    "condition": "only if what is judged is one change proposed against a package that is not the core — a pull request, a patch, a branch — rather than the repository as a whole",
    "rulesQuery": "",
    "checklist": [
      "Establish the change before any lookup: the changed paths, the diff, the branch it targets, the commit messages, and whether it merges and what the repository's own pipeline said. Every lookup below takes one of them as its argument, and this server reads neither your git state nor the forge the change arrived on.",
      "Enumerate what the diff removes or renames before judging what it adds. A package's public surface is not only PHP — a TCA field, a TypoScript path, a Fluid partial or section, a ViewHelper argument, a label key, a signature somebody else's override calls — and that finding class is the one a reading of the new code does not surface, because the evidence is in what is gone.",
      "Judge it on every major the package's Composer constraint declares and not only on the installed one. The installation supplies one point in that range, and a finding established there is established for that point.",
      "Say which convention the commit message was judged against. The repository's own log decides it, and reporting a subject as an error because it carries no TYPO3 keyword is a finding about a repository nobody is reviewing.",
      "Order the report by what stops the change being merged, and close on the review surfaces with each marked assessed, unassessed or not applicable to this diff. A review that reports only findings cannot be told apart from one that looked at less.",
      "Every finding names the changed path it is about. A statement about the package that does not tie to a line in this diff belongs in an issue rather than in a review of one change."
    ],
    "checks": [],
    "tools": [
      "typo3_project_describe, for the TYPO3 and PHP versions the package declares and which of its commands are checks",
      "typo3_hint_lookup with the changed paths, one call per subsystem the diff touches",
      "typo3_documentation_lookup with targetVersion per declared major, for the surfaces a changelog's silence cannot answer for",
      "typo3_commit_message_guide with workflow=\"project\", for the convention the message is held to where the repository writes TYPO3 keywords"
    ]
  },
  {
    "id": "distribution-content",
    "title": "Shipping a site's content in a package",
    "skill": "typo3-distribution-content",
    "match": [
      "distribution",
      "initial content",
      "seed the page tree",
      "ship the content",
      "starter content",
      "import the page tree"
    ],
    "matchWeak": [
      "export the content",
      "data.xml"
    ],
    "condition": "only if the content of a site is to travel inside a package rather than to be produced in the installation it is edited in",
    "rulesQuery": "",
    "checklist": [
      "Read the seed back out of the installation before exporting it. A record takes the defaults its configuration gives it rather than the ones its columns carry, and one of those decides whether a page is delivered to a visitor at all; where it is wrong, correct the script rather than the records, because the script is what the next version of the artifact comes out of.",
      "The export command reporting success is not evidence the export worked. It reports the same success for an artifact that left every image out and for one that left a whole table out, and both are invisible until somebody installs the package, so what is checked is the artifact.",
      "Put the artifact and the directory of files beside it into the extension under the names the import looks for. Neither arrives there by being asked for: the export keeps only the last segment of the name it was given.",
      "Ship the site configuration as the whole directory the site keeps it in, not the one file that is obviously configuration. What the installation put beside it is part of the site, and left behind the receiving installation resolves the site, finds every page and renders nothing.",
      "Prove it on an installation that has never had this package, with the package active before the install runs: the records arrived, the site answers on that installation's address and renders, and every page answers rather than only the one at the root. The import is remembered, so re-importing where it came from proves nothing."
    ],
    "checks": [],
    "tools": [
      "typo3_hint_lookup with id=sitepackage-initial-content, for what an extension ships content as",
      "typo3_documentation_lookup at the version being shipped, for which records belong in the artifact and which are the receiving installation's own",
      "typo3_configuration_lookup, for what the receiving installation actually merged rather than what it was given",
      "typo3_commit_message_guide with workflow=\"project\", for the message the artifact and the site configuration are committed with"
    ]
  },
  {
    "id": "content-relaunch",
    "title": "Carrying an existing body of content onto TYPO3",
    "skill": "typo3-content-element-development",
    "match": [
      "relaunch",
      "migrate content",
      "content migration",
      "migrate the existing content",
      "take over the content",
      "carry the content over",
      "existing content"
    ],
    "matchWeak": [
      "legacy",
      "migrate",
      "import the content",
      "drupal",
      "wordpress",
      "crawl the site",
      "scrape"
    ],
    "condition": "only if a body of content already exists somewhere else and is being carried into TYPO3, rather than code being upgraded inside one",
    "rulesQuery": "",
    "checklist": [
      "Decide what the content elements are before writing anything that reads the old site. An import writes whatever model it is handed and one blob per page is a model, so the elements are settled from how an editor will maintain the result rather than from how the source is marked up.",
      "Read the legacy pages for the shapes that recur — a team list, a fee table, a bullet list with links — rather than for what a crawler would have to parse. Those shapes are the elements, and what the source cannot express is exactly where the new model is decided.",
      "The core html element is not a migration target. Its template renders bodytext through f:format.raw, so whatever the import stored is handed to the visitor as it stands, and a page that arrived as one of them has been pasted rather than migrated.",
      "Count what the migration will produce before it runs. The count is knowable from the source, and it decides where the records are maintained and what renders them — both of which are retrofitted at a cost once the import has filled the table.",
      "The installation is the scaffolding the model lands in. Setting it up is not the question this task opens with, and a session that starts there designs the content model out of whatever the scraper produced."
    ],
    "checks": [],
    "tools": [
      "typo3_hint_lookup with id=content-elements, for what an element owns and how the record type is registered",
      "typo3_extension_describe, for what the target installation already registers to import into",
      "typo3_hint_lookup with id=extbase, for whether what the count describes is a plugin rather than a data processor"
    ]
  },
  {
    "id": "installation-setup",
    "title": "Setting an installation up",
    "skill": "typo3-development-installation",
    "match": [
      "typo3 setup",
      "set up a new",
      "set up an installation",
      "install typo3",
      "fresh installation",
      "bootstrap the installation",
      "site from scratch"
    ],
    "matchWeak": [
      "setup",
      "installing typo3",
      "development installation"
    ],
    "condition": "only if the task is setting an installation up rather than working on the code in one",
    "rulesQuery": "",
    "checklist": [
      "State the admin username and the password in your reply, with the URL of the backend they belong to. The setup command prints neither, so where it ran non-interactively the party that chose the value is the only one that has it — and a password nobody reports is a lockout of the installation that was just created.",
      "Generate that password rather than inventing a quiet default, and keep it out of the shell history: --admin-user-password and TYPO3_SETUP_ADMIN_PASSWORD both end up in it, which is what the command's own help warns about. The interactive mode avoids it, and so does a leading space in bash and zsh.",
      "The same value becomes the install tool password. The command creates the backend user and then sets BE/installToolPassword in config/system/settings.php to what it was handed, and there is no option of its own for it — so the two start out identical and diverge silently the first time somebody changes the backend password.",
      "Neither is stored reversibly, so recovery is a reset and not a lookup: backend:resetpassword for the backend user, and for the install tool the BE/installToolPassword hash in config/system/settings.php — which install:password:set rewrites where the installation has that command.",
      "Leave --create-site out where a sitepackage ships a root page and a site configuration. It creates both itself, and the sitepackage's own import then lands beside them: a second root page, and its site configuration skipped because the identifier already exists.",
      "Settle what the command takes before writing the command line: typo3_hint_lookup with id=installation-setup says which connection types --driver accepts against the driver name that lands in config/system/settings.php, that sqlite needs no credentials at all, that a database with tables in it is refused, and that --create-site and --distribution do nothing in Composer mode wherever a required package ships an Initialisation/ data file — which is where an installation ends up with no site configuration and no way to be told its own URL."
    ],
    "checks": [],
    "tools": [
      "typo3_project_describe, for what the installation around this repository already is",
      "typo3_hint_lookup with id=installation-setup, for what typo3 setup accepts, what it refuses, and which of its options are inert in Composer mode",
      "typo3_hint_lookup with id=sitepackage-initial-content, for what a sitepackage seeds an empty installation with and why the order of the two setup commands decides it"
    ]
  },
  {
    "id": "installation-upgrade",
    "title": "Upgrading or maintaining an installation",
    "match": [
      "upgrade",
      "maintain",
      "maintenance",
      "composer update",
      "new major"
    ],
    "matchWeak": [
      "updates"
    ],
    "condition": "only if the task is about an installation rather than about the code in it",
    "rulesQuery": "",
    "checklist": [
      "Establish where you are before changing anything: typo3_project_describe for the installed TYPO3 and PHP and for which extensions are the project's own, typo3_changelog_lookup with type=breaking for what the target version did to them.",
      "Check every third-party extension for a release that supports the target major. One that has none decides the schedule, and Composer reports it as a conflict rather than as advice.",
      "Dump the database and work on a copy. From the first schema change on, the upgrade cannot be reverted with git.",
      "Raise the PHP version where the target requires it — in composer.json and in the environment that runs it — then require the new TYPO3 constraints together with every extension in one composer call.",
      "Then, in this order: typo3 extension:setup for the schema and the static data, typo3 upgrade:list and typo3 upgrade:run for the wizards, typo3 cache:flush.",
      "Run the Extension Scanner over the project's own extensions for the call sites the changelog named, and re-check the site configuration, the TypoScript and the TSconfig — then repeat the same commands in the same order on every other environment."
    ],
    "checks": [],
    "tools": [
      "typo3_project_describe, for what this installation is and which commands this repository declares",
      "typo3_changelog_lookup with type=breaking or deprecation and the target version",
      "typo3_hint_lookup with id=installation-upgrade, for the order of operations and why it is that order"
    ]
  },
  {
    "id": "installation-operations",
    "title": "Bringing an installation up and running it",
    "skill": "typo3-development-installation",
    "guide": "project/installation/booting-a-clone",
    "changesNothing": true,
    "match": [
      "operations",
      "boot a",
      "boot the",
      "boot up",
      "booting",
      "spin up",
      "start the environment",
      "start the local environment"
    ],
    "matchWeak": [
      "ddev start",
      "seed",
      "development instance",
      "local instance",
      "fresh clone",
      "import the database"
    ],
    "condition": "only if the task is bringing an installation up or running one rather than changing the code in it",
    "rulesQuery": "",
    "checklist": [
      "In a DDEV project the lifecycle hooks in .ddev/config.yaml — post-start, post-import-db, pre-pull, post-pull — and the pull providers under .ddev/providers/ are the install and the import: they are what installs the dependencies, updates the schema and creates the backend user, so equivalent commands typed beside them boot something this repository does not describe. typo3_project_describe reports the environment and the composer scripts, and those hooks are read from the file itself.",
      "Where no installation exists yet, the order is typo3 setup for config/system/settings.php, the admin user and the schema, then typo3 extension:setup for the extension setup and the content a required distribution package imports, then typo3 cache:flush. The setup command refuses a database that already holds tables, so a script that runs a second time needs its own guard on settings.php — --force rewrites the settings files and not the schema.",
      "Seed a distribution by requiring its package and running typo3 extension:setup. Since v14 the setup command's --distribution and --create-site are inert in Composer mode as soon as a required package ships Initialisation/data.xml: every Composer-required package counts as an active distribution, so the command reports that the options have no effect and writes no site configuration at all.",
      "Since v14 a site configuration that arrives with an import lands on base /<identifier>/, because Import::processSiteConfigurations() overwrites what the export carried — the importer cannot know the URL of the installation it is importing into. A seeded installation therefore answers 404 at its own root until that base is corrected in config/sites/<identifier>/config.yaml, and the cause is not in the distribution package.",
      "A second site under the same host is the other way a frontend answers 404 while the backend works. TYPO3 matches every site's routes at once and a base carrying a host beats a bare path, so the site on base: / loses every request to the one that names the host, whatever the two paths are. Give the second site that host in its base as well, and read every site's base with typo3_project_describe before treating the message as a slug problem.",
      "A site nobody wrote is the fourth, and its identifier says so: the core writes one called autogenerated-<uid>-<md5 of that uid> whenever a page is created at pid 0 or with is_siteroot set, on a base that is the URL of the request which created it plus /autogenerated-<uid>. So an installation set up through the backend answers at that sub-path rather than at its root, and the base is corrected in config/sites/<identifier>/config.yaml like any other. Read the identifier before reaching for the import: that hook does not fire while an import is running, so the two never produced the same site.",
      "Leave config/system/additional.php to DDEV where DDEV writes it. While the #ddev-generated marker line is in that file it is rewritten by every start that finds an installed TYPO3, and it supplies the database connection, the container's ImageMagick paths, Mailpit and SYS/trustedHostsPattern — so hand-writing those is redundant, and switching settings management off costs the trusted hosts exception the router's host name raises. Where the installation does not use DDEV's database container, on SQLite or with omit_containers: [db], that generated mysqli connection overrides what typo3 setup wrote into settings.php: take the file over by removing the marker line and leaving its database section out.",
      "A clone is where that detection finds nothing: DDEV reads vendor/typo3/cms-core/Classes/Information/Typo3Version.php to decide the file belongs there at all, so the ddev start that necessarily precedes composer install writes no additional.php, and every request answers exception 1396795884 for the trusted hosts pattern while typo3 setup reports success. Start the environment again once the dependencies are installed, which ends the ordering for good — a post-start hook that installs them does not close it, because the detection runs before the hooks. Committing a project-owned additional.php is the other way out rather than the first: it is there from the first request, and it puts installation state under version control. A repository that deploys from the checkout already carries that state, and an extension repository does not — the Composer root is the package there and config/ is ignored whole, which typo3_hint_lookup with id=extension-repository-installation states.",
      "Verify by requesting the site and the backend rather than by reading a green start. DDEV's fail_on_hook_fail defaults to false, so a post-start hook that installs the instance can fail while ddev start reports success — an install hook belongs behind fail_on_hook_fail: true.",
      "Before the boot is reported done, establish how this package expects a fresh instance to be filled and name it to the user: the shipped Initialisation/ data file, a console command it ships, a site set an existing site has to depend on, or none of them — in which case the procedure is written down only in the package's own manual, and typo3_extension_describe reports where that manual is. An installation that answers and holds nothing to look at is not yet what was asked for, and for many extensions the step is a backend module rather than anything the console can do."
    ],
    "checks": [],
    "tools": [
      "typo3_project_describe, for what this repository declares: the TYPO3 and PHP it runs, the environment it ships, and the commands in its composer.json",
      "typo3_hint_lookup with id=installation-boot, for what a clone owes the installation before it answers: the schema an imported database is behind on, the caches it brought with it, the backend user nobody has the password for, the host the site configuration names, and where the files were expected",
      "typo3_hint_lookup with id=project-configuration-files, for which file configures what in an installation, and with id=extension-asset-build for the build a repository declares",
      "typo3_hint_lookup with id=fresh-instance-seeding, for the ways a package declares that it fills a fresh instance and for what holds when it declares none",
      "typo3_hint_lookup with id=site-base-collision, for which of two sites answers a request when both bases fit it, and for what the 404 that shows it does and does not say",
      "typo3_hint_lookup with id=autogenerated-site-configuration, for where a site nobody wrote came from, what the core puts in it besides the sub-path base, and which page types it is written for",
      "typo3_hint_lookup with id=page-not-found-within-a-site, for what the not-found means once a site did answer: which message is a path the router never resolved and which is a page it withheld, and what a hidden or deleted root page does to the tree under it",
      "typo3_extension_describe, for what the extension being booted ships: the Initialisation/ data files, the console.command tag, the site sets and where its manual is"
    ]
  },
  {
    "id": "content-element",
    "title": "Adding or changing a content element",
    "skill": "typo3-content-element-development",
    "match": [
      "content element",
      "CType",
      "tt_content",
      "backend preview"
    ],
    "matchWeak": [
      "carousel",
      "slider",
      "accordion",
      "slides",
      "teaser"
    ],
    "condition": "only if the task adds or changes a content element rather than the page around it; an Extbase plugin is one of them and not the other category",
    "rulesQuery": "",
    "checklist": [
      "Describe the editor workflow before naming a single field: who creates each piece of this element, in which order, and what translating, hiding and deleting the element does to it. The data model is the answer to that, not a choice made afterwards.",
      "Settle whether this element needs Extbase before its rendering is designed. It is not the choice between a content element and a plugin: since v14 registerPlugin() puts a plugin into the same CType selector as any other element, and configurePlugin() renders it as lib.contentElement with an EXTBASEPLUGIN at 20. What the fork asks is whether a controller has to answer the request — reading records into a template needs no model, no repository and no controller, and pagination, a search with arguments, validation and forms are what Extbase is for. Both sides of it are their own hint: typo3_hint_lookup with id=extbase, and with id=frontend-records.",
      "Read the answer to that off the extension rather than deciding it alone: typo3_extension_describe reports each content element with a kind of element or plugin, and a kind of plugin is the architecture this extension already has — models, repositories, controllers. Building the next element on TCA and a data processor departs from that convention, which is a decision to state rather than one to make by not noticing.",
      "Own repeatable content in a child table of its own with type=inline. Referencing existing records is right where reuse was asked for and nesting arbitrary tt_content where nesting was; neither is a way to store a list of slides, and both are technically possible, which is why an element that skipped the workflow question ends up with one of them.",
      "Register this element in a file of its own under Configuration/TCA/Overrides/ and give it its own TypoScript file and template, so the generic files stay what is genuinely shared.",
      "Load the CSS and JavaScript only this element needs through the Fluid AssetCollector in its template, rather than on every page of the site.",
      "Cover the persistence of the child records and the order they render in with functional tests. A unit test of mocks asserts the mock, and browser behaviour that was not tested is reported as untested."
    ],
    "checks": [],
    "tools": [
      "typo3_hint_lookup with id=content-elements, for what the element owns and how the record type is registered, then with id=extbase and id=frontend-records for the two sides of the Extbase fork",
      "typo3_extension_describe, for the architecture this extension already has: each content element it registers with a kind of element or plugin, and the templates they render through"
    ]
  },
  {
    "id": "tca-field",
    "title": "Adding or changing a field on an existing table",
    "match": [
      "tca",
      "showitem",
      "palette",
      "ext_tables.sql",
      "database column"
    ],
    "matchWeak": [
      "record form",
      "backend form",
      "form field",
      "add a field",
      "new field"
    ],
    "condition": "only if the task adds or changes a field on a table that already exists; a new table, and the content element or backend module that owns one, are the neighbouring categories",
    "rulesQuery": "",
    "checklist": [
      "Name the table and the type before the field. A column in the columns section exists in the schema and appears in no form until a type's showitem puts it there, and a table with several types shows a different form per type — so \"add the field\" is two edits and the second one is the one that is forgotten.",
      "Read what the table already has rather than adding beside it: typo3_extension_describe reports what this extension registers, and a field whose job is already done by an existing column is a second way to store one thing.",
      "Settle the database column with typo3_schema_lookup rather than by writing ext_tables.sql first. It lists what TYPO3 derives from the TCA itself, with the Doctrine type, the NOT NULL and the default — which is also exactly what an ext_tables.sql does not have to declare, so a redundant declaration is found there instead of in a review.",
      "Pass the XLF resource used at the consuming code to typo3_label_lookup before wording the field's label. A match in another resource is not reusable there, and a guessed label key fails only at runtime.",
      "The conventions are typo3_hint_lookup with id=tca-formengine for the form side and id=tca-schema-api for what the schema API answers about a field; a datetime column has its own, id=tca-datetime-storage, because what it stores and when it is null is not what the type name suggests."
    ],
    "checks": [],
    "tools": [
      "typo3_schema_lookup, for the column this field produces and whether ext_tables.sql has to declare it at all",
      "typo3_hint_lookup with id=tca-formengine and id=tca-schema-api, for the conventions the field is written under",
      "typo3_extension_describe, for what the table and the extension around it already register"
    ]
  },
  {
    "id": "site-setting",
    "title": "Adding a setting to a site set",
    "match": [
      "site set",
      "settings.definitions.yaml",
      "site setting",
      "settings.yaml",
      "add a setting",
      "add the setting",
      "per site"
    ],
    "matchWeak": [
      "setting",
      "typoscript constant",
      "constants"
    ],
    "condition": "only if the task adds or changes a setting a site set defines; the site configuration itself — its languages, its base, its error handling — is not this and is not covered",
    "rulesQuery": "",
    "checklist": [
      "Decide who the value is for before deciding where it goes. A value an integrator changes per site is a set setting; a value that never differs between sites is the package's own and needs no setting at all. What it is not is a TypoScript constant: sets arrived in TYPO3 v13 and settings.definitions.yaml replaced constants there, so a package on that line or above writes a setting and a package still serving v12 has no set to put one in — typo3_hint_lookup with id=site-set-settings carries the range each statement holds on.",
      "Define it and give it a value in two different places. The definition — its type, its default, its label — belongs to the set that owns it, and what a site actually runs with is the value that site's configuration supplies; a default that is only ever written once is a definition nobody had to make.",
      "Say which set owns it. A package that ships several sets has to answer why this setting sits in this one, and a setting a dependent set needs is a setting the depended-on set defines.",
      "The conventions are typo3_hint_lookup with id=site-sets for what a set is and id=site-set-settings for the settings themselves; a package moving constants over asks id=site-set-migration, which is about what the move leaves behind rather than about the new setting."
    ],
    "checks": [],
    "tools": [
      "typo3_hint_lookup with id=site-set-settings, then id=site-sets for the set around it",
      "typo3_project_describe, for the sites this project configures and the sets they already depend on"
    ]
  },
  {
    "id": "event-listener",
    "title": "Registering an event listener",
    "match": [
      "event listener",
      "aseventlistener",
      "psr-14",
      "psr 14",
      "event dispatcher",
      "without overriding",
      "instead of overriding",
      "do not want to override"
    ],
    "matchWeak": [
      "listener",
      "event",
      "hook"
    ],
    "condition": "only if the task listens to an event something else dispatches; dispatching a new event from your own code is the other half of the subject and is not this",
    "rulesQuery": "",
    "checklist": [
      "Find the event that is really dispatched before writing a listener for it. An event class that reads plausibly and is dispatched nowhere is a listener that never runs and throws nothing, which is the failure this task shape has instead of an error.",
      "How a listener is registered is bound to the TYPO3 line and is not stated here: typo3_hint_lookup with id=events-extension-points carries both mechanisms with the versions each holds on, and a package serving two majors gets both from it.",
      "Say what happens when another listener has already run. Ordering is declared or it is not there, and a listener that quietly assumes it goes first is correct until somebody installs a second extension.",
      "A hook is not the fallback. Where a subsystem still has hook-based extension points that is a fact about that subsystem — ask its own hint with the intent, and take the narrower event where there is one."
    ],
    "checks": [],
    "tools": [
      "typo3_hint_lookup with id=events-extension-points, for the registration each covered line has and what an event class owes its listeners",
      "typo3_extension_describe, for what the extension dispatching the event already registers"
    ]
  },
  {
    "id": "asset-build",
    "title": "Asset build",
    "skill": "typo3-extension-asset-build",
    "guide": "any/assets/how-an-asset-reaches-a-page",
    "match": [
      "npm",
      "package.json",
      "dependabot",
      "webpack",
      "vite",
      "grunt",
      "sass",
      "asset build",
      "rebuild the assets",
      "built css",
      "built javascript"
    ],
    "rulesQuery": "asset reaches a page",
    "checklist": [
      "Run the build commands as typo3_project_describe reported them, in the directory the manifest they came from sits in and on the Node it names.",
      "Verify a core class or icon the output borrows before writing it, one call per declared major, and read where the class sits rather than that the name exists.",
      "After the rebuild, check every mapped path in Configuration/JavaScriptModules.php against the file the build wrote, and the route each frontend output reaches a page by.",
      "Commit the rebuilt artefacts together with the source that produced them, where the repository tracks its output."
    ],
    "checks": [],
    "tools": [
      "typo3_component_lookup with the targetVersion of each declared major, for where a borrowed class sits",
      "typo3_rule_lookup with documentId=\"any/assets/how-an-asset-reaches-a-page\", for the route an output reaches a page by and what proves it still carries",
      "typo3_hint_lookup id=public-assets, for how Resources/Public is published and what makes a file resolvable at all"
    ]
  }
]
