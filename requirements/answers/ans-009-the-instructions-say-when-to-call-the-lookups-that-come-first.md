---
id: R-ANS-009
title: 'The instructions say when to call the lookups that come first'
status: held
heldBy:
  - ScopeTest::theScopeInstructionsOrientTheClientBeforeItsFirstCall
---

# R-ANS-009 — The instructions say when to call the lookups that come first

**Initialize instructions say when to call the three lookups whose value exists
only before a runtime-only mistake happens.**

Components before backend markup, icons before the choice of an identifier, and
labels before the session adds or rewords one.

## From

A comparison with a server that tells the agent what to call before its first
question instead of a reliance on tool descriptions (2026-07-30).
