---
id: R-FBK-003
title: 'A forward run is a record, not a remembered status'
status: held
heldBy:
  - ScenariosTest
---

# R-FBK-003 — A forward run is a record, not a remembered status

**A forward run of a review is a recorded result rather than a status line
somebody remembers.**

The prose stays the only copy of the prompt and the criteria. The run adds where
it happened, which server it ran against, which skills activated, which tools
the session called and with what arguments. It adds one judgment with evidence
per criterion. The verdict follows from those judgments instead of a statement
beside them. The run carries a digest of the criteria that judged it. So a
scenario reworded after its run says so rather than keeps the old answer.

## From

`EXT-04` stood at `partial` next to a `SITE-07` marked `covered` that it shares
its subject with, with no run behind either mark (2026-07-30).
