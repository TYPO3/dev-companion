---
id: R-KNW-046
title: 'impexp is the way a page tree is established again'
status: held
heldBy:
  - HintsTest::theSeedingAnswerNamesImpexpAsTheWayATreeIsEstablishedAgain
---

# R-KNW-046 — impexp is the way a page tree is established again

**The seed answer names impexp as what builds a site or page tree again. A seed
script is what a session writes for content that exists nowhere yet.**

The export is the artifact a distribution ships and imports again, not the
residue of a one-off run.

## From

The corpus said "seed with DataHandler, then export" and described the script as
a throwaway. That reads as if impexp were the leftover rather than the product
(2026-08-03).
