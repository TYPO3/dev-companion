---
serves: [feedback/2026-08-31-233757-nothing-covers-renaming-an-extension-key-table.md]
priority: normal
---

# Write the document for renaming an installed extension

Ladder step 1a: the probe reaches nothing, and the session established the whole
of it by breaking each part — the tables `extension:setup` creates beside the
old ones, `INSERT ... SELECT *` mapping positionally between two tables
generated from one TCA, `sys_file_reference.tablenames` and `sys_refindex`
carrying table names as data, and a seeding command that composed a CType from a
prefix and wrote 118 records back after the literals were replaced. Verify each
against `.checkouts/` before writing
`project/refactoring/renaming-an-installed-extension`, and write the check that
proves it as a query over the old prefix rather than a green suite.
`installation-boot` is the neighbouring entry and this is its mirror.
