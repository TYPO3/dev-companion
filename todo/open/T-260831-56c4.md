---
serves: [feedback/2026-08-31-233327-no-hint-covers-restricting-the-ctype-selector.md]
priority: normal
---

# Write what restricts the CType selector, and say when nothing matched

Ladder step 1a: `bin/cli hints:probe` for the query reaches nothing, and the six
hints the session got back — content-elements, tsconfig, tca-formengine and
three more — gave it no way to tell. Establish against `.checkouts/` and the
TypoScript reference what `TCEFORM.tt_content.CType.keepItems` and `removeItems`
do, and what unsetting the TCA items and the `types` entry does instead,
including what happens to a record already stored under a removed type; then
write the hint. The second half is the answer shape and is its own step: where
every hint of a match is well below what counts as being about the question, the
answer says so instead of reading like six that answer it.
