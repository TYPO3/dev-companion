---
serves:
  - feedback/2026-08-31-234323-nine-of-eighteen-tools-never-called-and-three.md
  - feedback/2026-08-31-234521-why-the-unused-tools-stayed-unused-six-causes.md
  - feedback/2026-08-31-234553-correction-the-deferred-tool-barrier-was-not.md
priority: high
---

# Have an answer name the tool that answers the next question

Ladder step 1b, the shape: nine of eighteen tools went uncalled in one session
and three of them cover work done by hand and got wrong — the FlexForm field
name found by grep, two tables' column order diffed by `SHOW COLUMNS`, the
settings read out of the YAML. The session's own correction says the deferred
tool list was not the barrier and that the one lever is a tool named inside an
answer the caller is already reading, the way the `documents` array works. The
step is `ExtensionDescribe`: it already reports `tcaTables` and
`unlistedFlexForms`, so those two lines name `typo3_schema_lookup` and
`typo3_flexform_lookup` with the argument to pass. It touches a declared
`outputSchema`, so the shape is decided before it is written — a field beside
the list, not a sentence in the text.
