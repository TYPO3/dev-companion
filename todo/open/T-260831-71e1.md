---
serves: [feedback/2026-08-31-234521-why-the-unused-tools-stayed-unused-six-causes.md]
priority: normal
---

# Say in an answer that a value is the resolved one

What a hint carries beside its statements was settled on 2026-09-01 —
`D-KNW-138`, it is a statement — and two of the three feedback are archived with
it. This is the third cause of the one that is left: a session read the settings
file with `sed` and `grep` while changing it, three times, and felt the question
answered, because a file returns something too. `typo3_configuration_lookup`
says in its description that it answers the runtime value after every extension
has had its say, and its answer text says so only about the provider order. The
step is one line in the answer naming what the value is not — the layer a
settings file carries on its own — which needs an installation to be read
against, so `bin/cli environment:create` comes before the edit.
