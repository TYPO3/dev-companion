---
serves: [scenarios/]
priority: normal
---

# Have a review name the failure it shares instead of wording it again

Edit the **How it fails** of `review-01-site-project.md`,
`review-02-reusable-extension.md` and `review-03-core-patch.md` in
`scenarios/forward/` so each names the three shared failures where
`scenarios/forward/readme.md` states them, and words only what is its own. The
maintainer chose that on 2026-09-19 over waiting for a re-run that comes for
another reason. The digest check then sets each recorded run to `unrun`, and
`ScenariosTest` stays red until a fresh forward run is recorded per review,
`documentation/records/forward-runs.rst`. So the edit ends with this todo in
`waiting/`, on the three runs, which are a session in an environment nothing
here can drive.
