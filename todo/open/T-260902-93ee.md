---
serves: [D-DOC-002]
priority: low
---

# Three skill sentences past 80 words are lists

`bin/cli prose:check` names them: `typo3-development-installation` lines 252-261
("Five lookups own what it can be: …", 112 words, the longest in the corpus),
`typo3-core-patch-checkout` 275-291 ("This skill owns getting a change under
review into a checkout and back out of it: finding it, fetching …", 87 words)
and `typo3-core-patch-development` step 3's second sentence ("Three parts of
that answer are not in the description …", 74 words). Each is a list of ids or
verbs jammed into one sentence: one bullet per id or per verb, and step 3 of the
patch skill split into "read the issue" and "what the reading produces". The
tests hold the sentences by their opening words, so read `SkillTest` for each
before rewrapping.
