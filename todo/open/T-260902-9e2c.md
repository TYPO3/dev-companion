---
serves: [skills/]
priority: normal
---

# Four tools and three documents no skill reaches

Read on 2026-09-02: no skill routes to `typo3_permalink_lookup` — while
`typo3-extension-documentation` says "Prefer the links it returns over
hand-built documentation URLs", which is that tool's job — nor to
`typo3_reference_list`, `typo3_ter_lookup` (the health audit's package surface)
or `typo3_snapshot_scope`. No skill hands over
`core/contribution/committed-build-output` (a core patch touching committed JS
or CSS belongs in `typo3-core-patch-development`'s "Make the change"),
`any/icons/drawing-a-content-icon` (the content element skill routes
`typo3_icon_lookup` alone) or
`project/refactoring/renaming-an-installed-extension`. Each is one sentence at
the step that needs it, held by `ROUTING_SKILLS` where it is a tool; the last
document is a task shape no skill owns and is a candidate only where a run shows
it, per "Earning a skill".
