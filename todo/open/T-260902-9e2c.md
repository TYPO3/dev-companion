---
serves: [skills/]
priority: normal
---

# The snapshot scope is routed from no skill, and may not survive to be

Read on 2026-09-02: no skill routes to `typo3_snapshot_scope`, whose answer
judges whether a `typo3_component_lookup` miss is authoritative. The skills that
route the component lookup are `typo3-backend-module-development` and
`typo3-content-element-development`, so the sentence belongs at whichever of
those steps reads a miss.

**This waits on `T-260902-e940`**, which asks whether the tool is kept at all:
it offers retiring it in favour of putting `verifyCommand` into the component
miss, and a routing written now would be a sentence in two published skills
against a tool that is being withdrawn. Where that todo keeps the tool, this is
one sentence at the component step; where it retires it, this is deleted with
it.

The rest of what this file carried was done on 2026-09-02:
`typo3_permalink_lookup` is routed from `typo3-extension-documentation`,
`typo3_reference_list` from `typo3-extension-testing`, `typo3_ter_lookup` from
`typo3-extension-health`, `core/contribution/committed-build-output` from
`typo3-core-patch-development` and `any/icons/drawing-a-content-icon` from
`typo3-content-element-development`, each held by `ROUTING_SKILLS` where it is a
tool. `project/refactoring/renaming-an-installed-extension` is not queued: it is
a task shape no skill owns, and a candidate only where a run shows it, per
"Earning a skill".
