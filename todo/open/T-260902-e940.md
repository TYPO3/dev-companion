---
serves: [D-ANS-072]
priority: low
---

# typo3_snapshot_scope answers what the component miss already carries

A component answer requires `componentSource` and `catalog` — the provenance
with `installedVersion` and `skew` — and `typo3_snapshot_scope` adds only
`verifyCommand` and `counts`. The routing entry "A catalog lookup found nothing
… typo3_snapshot_scope, then typo3_feedback_record" and `Provenance::MISS_NOTE`
send a caller on a round trip for what the miss holds, and
`feedback/2026-08-31-234323` lists the tool among the never-called. Decide
between putting `verifyCommand` into the component miss and retiring the tool (a
name clients know, so the retirement is a release note and a `doesNotCover`
entry) and keeping it with the routing entry and the miss note dropped. Either
way the pair row in `ScopeTest` moves with it.
