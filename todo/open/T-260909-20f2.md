---
serves: [feedback/2026-09-09-182615-on-a-core-checkout-commands-lists-the-npm.md]
priority: normal
---

# Mark the npm entries a core checkout declares with the dispatcher that owns them

In `ProjectDescribe`, where `kind` is `core-checkout`, give each command whose
invocation is npm the note that the core runs it through
`Build/Scripts/runTests.sh -s <suite>` and that invoking npm directly skips the
PHP version and the database service. The mark goes on the entry rather than
beside the array, which is the whole of `D-ANS-152`: the sentence above the list
has stood since 2026-08-04 and a session read past it to the list. Nothing about
the core is left to look up — the four covered majors were read in `.checkouts/`
on 2026-09-09 and the decision carries what they say. `ProjectTest` is where the
mark is held, on a core checkout and on the absence of one elsewhere.
