---
serves: [D-DOC-070, requirements/, decisions/]
priority: normal
---

# Rewrite `requirements/` and `decisions/` in STE, then move the lead check

The records are the largest corpus: 26,201 sentences on 2026-09-14, and 402 of
the 924 leads over 25 words. `D-DOC-070` says a record changes form and never
substance: keep every number, every id, every date and every dated section.

`requirements/` is done since 2026-09-14: 246 files, zero sentences over 25
words that a session wrote, zero leads over 25. Below `decisions/`, `code/`,
`versions/`, `catalog/`, `evidence/`, `scope/`, `answers/`, `audience/`,
`discovery/`, `documentation/`, `feedback/`, `guides/` and `knowledge/` are
done. What remains is `task-skills/`, 94 files. The passive count `prose:check`
reports for a group readme is its title list, and a title stays. A title is a
file name and a link target across the corpus, so the sweep leaves titles as
they are; a quoted sentence stays as its source wrote it.

1. Run `bin/cli prose:check` and take the rows for `decisions/` as the worklist,
   one group at a time.
2. Rewrite the lead first, then the body. Reread the lead after the rewrite,
   because that sentence is what a reader stops after.
3. Run `bin/cli decisions:check` and `composer test` after each group. Commit
   each group on its own.
4. When no lead runs past 25 words, set `Prose::HELD` to `Prose::MEASURE` and
   delete the constant. Record that in a **Since then** on `D-DOC-070`.

Done when `bin/cli prose:check` names no file below the two directories and the
lead check fails at 25.
