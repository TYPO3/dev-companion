---
serves: [D-DOC-070, documentation/]
priority: normal
---

# Rewrite `documentation/`, `readme.md` and `CLAUDE.md` in STE

The site and the two files at the root describe the server outward. The measure
is 25 words a sentence — `D-DOC-070`.

1. Run `bin/cli prose:check` and take the `documentation/` rows as the worklist.
2. Rewrite one page at a time. Keep every `:doc:`, `:ref:` and label, and run
   `bin/cli links:check` after each page.
3. Run `bin/cli prose:format documentation/` and `composer test` after each
   page. `ProseTest` holds the format over the whole corpus.
4. Commit each page on its own.

Done when `bin/cli prose:check` names no file below `documentation/`.
