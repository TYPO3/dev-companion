---
serves: [feedback/2026-09-18-093132-javascript-unit-tests-hint-has-no-shape-for-a.md]
priority: normal
---

# The javascript-unit-tests hint says what a failing assertion on a rendered element does

Establish the two claims of the feedback that need a run, in a core checkout
with `Build/node_modules` installed and `runTests.sh -s unitJavascript` at hand.
First, the `oneEvent` typing of `@open-wc/testing-helpers` as the core pins it:
whether a two-argument call fails the build with TS2554, and on which covered
lines. Second, a failing chai assertion whose subject is a Lit-rendered element:
whether the file hangs until `testsFinishTimeout` and the reporter shows no
failure, and what decides it, read from @web/test-runner and @open-wc/testing
rather than from the symptom. Write what holds into
`knowledge/hints/testing.json` under `javascript-unit-tests`, bound with `since`
where it differs by line, and archive the feedback with
`bin/cli feedback:archive`. Judged on 2026-09-19 as step 1a and written into
`D-KNW-130`; the session that reported it paid four full suite runs, which is
what sets the priority.
