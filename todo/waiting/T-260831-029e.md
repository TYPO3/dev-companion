---
serves: [feedback/2026-08-31-233952-3101-records-in-a-storage-folder-and-nothing.md]
priority: normal
waitingOn: >
  Is the number of rows in a table on the model side of the boundary
  `D-AUD-010` draws, or on the record side? The feedback asks that
  `typo3_extension_describe` report how many rows each declared table holds, so
  a caller being oriented sees the scale that decides the editing surface. A
  count reads no field of any row, which is the whole of what `doesNotCover`
  names; it does run over the data with the shell user's access, which is the
  trust argument that entry rests on, and it says how much content an
  installation holds to whoever started the process. Waiting rather than
  building is recommended: the modelling half this feedback asked for landed
  without a count, because the number is knowable from the source before the
  import runs and the content-element skill now asks for it there — so what a
  count would buy is the case where the records already exist and nobody
  counted, which is one sighting. Building it also puts the first data query in
  a server whose probe has never run one.
---

# Say how many rows a table an extension declares actually holds

Judged on 2026-09-01 as `documentation/records/judging.rst` step 5: everything
worked as designed and the design is the price, so the reading went into
`D-AUD-010` and the question above is what it turns on. Where the answer is yes,
the step is what it costs — a count per declared table, against a database this
server has never queried, in an answer delivered before a task is known — and it
touches a declared `outputSchema`.
