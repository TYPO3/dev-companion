---
serves: [feedback/2026-08-31-234745-told-four-times-that-code-is-english-i.md]
priority: normal
waitingOn: >
  Should `typo3_project_describe` state a code language, when the only thing the
  repository declares is the source language of its XLF files? That is what
  `typo3_extension_describe` already reports per file, and the step from it to
  "the code is written in English" is an inference rather than a reading: a
  project with English labels and German comments declares exactly the same
  thing. The other sources this feedback names carry nothing to read — an
  `.editorconfig` has no such field, and a contributing file states it in prose
  where it states it at all. Proposing not to build it is the recommendation:
  what would be added is a claim about the repository that the repository does
  not make, in an answer whose every other field is what a file says. Against
  that stands the report itself, which is one session and says of this failure
  that most of it is the model's — and the corpus half of it landed, so what is
  being weighed is the sentence alone.
---

# Say which language a project writes its code in

Judged on 2026-09-01 as `documentation/records/judging.rst`'s *proposed*: the
practice this feedback ends on is stated in
`verifying-a-change-against-the-installation`, and the tool half turns on the
question above. Where the answer is to build it, the step is which of the
sources is read and what the sentence may claim — it touches a declared
`outputSchema`, and `D-ANS-129` is the last field added to a describe answer and
what it rejected.
