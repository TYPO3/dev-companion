---
serves: [feedback/2026-08-31-234039-extension-describe-on-fluid-styled-content.md]
priority: normal
waitingOn: >
  Should `typo3_extension_describe` report the top-level TypoScript objects an
  extension's setup defines, given that reading them takes a TypoScript reader
  this repository does not have? The one the feedback names is not in a file
  this answer already lists: `fluid_styled_content`'s set carries a single
  `@import`, and `lib.contentElement` is defined two files further in, so
  reporting it means following `@import` — relative and `EXT:` — and finding the
  assignments through the brace nesting, the `>` clear and the `<` copy. Asking
  the container instead does not answer either: the assembled setup is built per
  site and per request rather than held in the container. Proposing not to build
  it is the recommendation, because the caller's real question — why a
  sitepackage depends on this extension at all — landed as a hint on the same
  day without any of it, and a half-parser of TypoScript is a wrong answer that
  looks like a right one.
---

# Report what a TypoScript setup defines, not only its file name

Judged on 2026-09-01 as `documentation/records/judging.rst`'s *proposed*, and
the feedback is trimmed to this half. Where the answer is to build it, the step
is what the reader may claim: the objects it found by following the imports it
could resolve, said as that rather than as what the extension defines. It
touches a declared `outputSchema`.
