---
serves: [scenarios/]
priority: normal
waitingOn: >
  two forward runs the maintainer fares on this branch, REVIEW-02 in `E-EXT`
  and REVIEW-03 in `E-CORE`, and a site checkout for REVIEW-01, which `E-SITE`
  has not had since 2026-08-06. The three reviews name the shared failures
  since 2026-09-19 on this branch, so their recorded runs stand judged against
  the old wording and `bin/cli scenarios:check` says so. The maintainer chose
  on 2026-09-19 to keep the branch and fare the two runs that have an
  environment; the branch comes home once those two are recorded, and
  REVIEW-01 stays `unrun` until a site checkout exists.
---

# Have a review name the failure it shares instead of wording it again

Fare the two runs on this branch, each the way
`documentation/records/forward-runs.rst` says. For REVIEW-02 in the checkout
`todo/reference/` names for `E-EXT`, and for REVIEW-03 in
`/home/benji/projects/typo3-cms`: `bin/cli scenarios:record <id> claude-code`
writes the empty run, `bin/cli scenarios:show <id>` prints the prompt to paste
verbatim, and the judgment per criterion goes into `scenarios/runs/<id>.json`
with the skills and the tool calls. Then set REVIEW-01's **Status today** to
`unrun` and take `scenarios/runs/REVIEW-01.json` out, because the site checkout
it needs is gone from this machine, and `bin/cli scenarios:check` accepts a
never-run review and refuses a stale run. When the check is green, bring the
branch home with `bin/cli todo:home T-260901-a4d8` from the checkout `main`
stands on.
