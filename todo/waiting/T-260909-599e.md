---
serves:
  - feedback/2026-09-09-180638-a-core-patch-review-ran-end-to-end-with-zero.md
  - feedback/2026-09-09-182500-three-matching-typo3-core-patch-skills-stayed.md
  - feedback/2026-09-09-184212-no-skill-activated-all-session-the-repo-s-own.md
priority: high
waitingOn: >
  Does the installer write into the project's own agent instruction file? That
  is the one channel `D-SKL-033` and `D-SKL-038` both name as untried, and both
  of their third **Wrong if** are now met: three more sessions on 2026-09-09,
  every skill listed and described in the request's own words, the change
  answer's routing tail read twice, and no activation. The answering side is
  spent and the description side is bounded by `D-SKL-092` — one of the four sessions routed
  past the client's own loaded edit tool because a harness instruction made Bash
  the default, so a deferred tool of this server's cannot win there. What raises
  the stakes is what beat it: the core's own `AGENTS.md` prescribes a curl route
  to forge.typo3.org for the question `typo3_forge_lookup` answers, it is
  checked in, and the session followed it. Writing into somebody else's
  instruction file is outward-facing and reversible only by them, which is why
  nothing here takes it. Three shapes to choose between, and the recommendation
  is the second: write nothing and accept the reach this server has; append a
  block the installer owns and rewrites, marked so a person can see what put it
  there; or offer it as a printed snippet the installer tells the maintainer to
  paste. The second is recommended because the installer already owns
  `.mcp.json` and the published skills in that project, and a block it rewrites
  goes stale the way those do rather than the way a paste does.

  Put on 2026-09-10 with the second recommended, and the answer was to wait.
  So nothing about the channel is settled and the three shapes are still
  what an answer chooses between. What would move it is a fourth session of
  the shape, or a client where the reach is different.
---

# The reach question: the project's own instruction file is the channel left

Put the question above to the maintainer, then take what comes back. An answer
that picks a shape moves this card into `open/` with that shape as its first
step and `src/Server/Installer.php` as the file. An answer that declines writes
the refusal and its reason into `D-SKL-033` under **Decided** and archives the
three feedback. The reach question then stands settled rather than asked a fifth
time.
